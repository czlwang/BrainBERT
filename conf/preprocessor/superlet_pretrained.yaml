#This config can only be used when the upstream is frozen
name: spec_pretrained #TODO check if these are the params that the model was pretrained with
spec_name: superlet
c1: 1
order_min: 3
order_max: 30
decim: 50
min_f: 0.1
max_f: 200
n_f_steps: 40 #foi = linspace(f_min, f_max, n_f_steps)
upstream_ckpt: ???
